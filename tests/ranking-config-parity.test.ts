import { describe,expect,it } from 'vitest'
import { OLD_APP_DEFAULT_CONFIG,positionPoints,decayMultiplier } from '../src/modules/rankings/config/default-ranking-config'
describe('paridade da configuração antiga',()=>{it('mantém pontos de posição',()=>{expect(positionPoints(OLD_APP_DEFAULT_CONFIG,1)).toBe(1000);expect(positionPoints(OLD_APP_DEFAULT_CONFIG,20)).toBe(16);expect(positionPoints(OLD_APP_DEFAULT_CONFIG,21)).toBe(16)});it('mantém pesos e bónus',()=>{expect(OLD_APP_DEFAULT_CONFIG.divisionWeights[1]).toBe(2.3);expect(OLD_APP_DEFAULT_CONFIG.competitionWeights.superleague).toBe(2);expect(OLD_APP_DEFAULT_CONFIG.nationalChampionBonus).toBe(300);expect(OLD_APP_DEFAULT_CONFIG.superleagueChampionBonus).toBe(400)});it('mantém decaimento',()=>{expect(decayMultiplier(OLD_APP_DEFAULT_CONFIG,2025,2025)).toBe(1);expect(decayMultiplier(OLD_APP_DEFAULT_CONFIG,2024,2025)).toBe(.85);expect(decayMultiplier(OLD_APP_DEFAULT_CONFIG,2020,2025)).toBe(.4)})})
